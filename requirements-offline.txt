# Core MCP Dependencies
mcp==1.21.0
anyio==4.11.0
httpx==0.28.1
httpx-sse==0.4.3
httpcore==1.0.9
pydantic==2.12.4
pydantic-core==2.41.5
pydantic-settings==2.12.0
starlette==0.50.0
uvicorn==0.38.0

# Additional dependencies for MCP
jsonschema==4.25.1
jsonschema-specifications==2025.9.1
referencing==0.37.0
rpds-py==0.28.0
python-multipart==0.0.20
python-dotenv==1.2.1
sse-starlette==3.0.3
PyJWT==2.10.1
cryptography==46.0.3
cffi==2.0.0
pycparser==2.23

# Standard library-like dependencies
certifi==2025.10.5
h11==0.16.0
sniffio==1.3.1
click==8.3.0
annotated-types==0.7.0
typing-inspection==0.4.2
typing_extensions==4.15.0

# LangChain Integration (Optional - for examples in clients/langchain/)
# Uncomment if you want to use LangChain integration
# langchain==0.3.0
# langchain-core==0.3.0
# langchain-community==0.3.0
# langchain-openai==0.2.0  # For custom API endpoints (qwen3-coder-480b, etc.)

# Local LLM Support (Optional - uncomment as needed)
# llama-cpp-python==0.2.0  # For llama.cpp integration
# ollama==0.1.0  # For Ollama integration

# Note: To create a wheelhouse for airgapped installation:
# 1. On a connected machine:
#    mkdir wheelhouse
#    pip download -r requirements-offline.txt -d wheelhouse
# 2. Transfer wheelhouse/ to airgapped environment
# 3. In airgapped environment:
#    pip install --no-index --find-links wheelhouse -r requirements-offline.txt
